/**
 * Client-safe duration budget thresholds — must stay in sync with `checkSceneDurationBudget`
 * in `scene-budget.ts` (server).
 */
export const SCENE_BUDGET_WARN_TOTAL_SECONDS = 90;
export const SCENE_BUDGET_MAX_TOTAL_SECONDS = 180;
