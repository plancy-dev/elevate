/**
 * Shared class strings for surfaces — tokens only (`docs/design/SYSTEM.md`, `globals.css`).
 * Use these for modal panels, popovers, and elevated strips so dark mode stays on semantic layers.
 */
export const modalPanelClassName =
  "rounded-xl border border-border-subtle bg-layer-01 shadow-card";
