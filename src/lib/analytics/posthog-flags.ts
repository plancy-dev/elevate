/**
 * PostHog feature flag keys (UPPERCASE_WITH_UNDERSCORE).
 * Create matching flags in the PostHog project before relying on them.
 */
export enum PostHogFeatureFlag {
  ELEVATE_DASHBOARD_BETA = "ELEVATE_DASHBOARD_BETA",
}
