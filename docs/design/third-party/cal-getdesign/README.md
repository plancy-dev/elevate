# cal (getdesign.md)

Upstream-style **DESIGN.md** produced by:

```bash
npx getdesign@latest add cal
```

- **Upstream index:** [getdesign.md — Cal](https://getdesign.md/cal/design-md)
- **Workflow context:** [What is DESIGN.md?](https://getdesign.md/what-is-design-md)

## This repo

| File | Role |
|------|------|
| [`DESIGN.md`](./DESIGN.md) | Snapshot from the CLI (for diffing when re-running `add cal`). |
| Repository root [`DESIGN.md`](../../../DESIGN.md) | **Project SoT** — customize here; agents read it next to `AGENTS.md`. |

Implementation mapping: [`../../elevate-cal-alignment.md`](../../elevate-cal-alignment.md). Do not edit the vendored file for product-specific decisions — change root `DESIGN.md` and tokens instead.

## Refresh

Re-run `npx getdesign@latest add cal` from the repo root, then compare `cal/DESIGN.md` (if recreated) to this file and merge any upstream wording improvements into root `DESIGN.md` deliberately.
