# S0 Baseline Placeholder

This file is intentionally created in S0.

Post-merge operator task:

- Capture Lighthouse metrics for `/` and `/dashboard`.
- Record at least LCP and CLS here with date, environment, and run profile.

Template:

```md
## YYYY-MM-DD

- Environment:
- Route `/`: LCP=?, CLS=?
- Route `/dashboard`: LCP=?, CLS=?
- Notes:
```
